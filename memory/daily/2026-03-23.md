# 2026-03-23

## Decisions

## Corrections

## Commitments

## Open Loops

## Notes

## [session]

- Fixed SonarCloud S6582 (background.js:170): replaced `!credentials || !credentials.server` with `!credentials?.server` in `warmupConnection()`. One-line change, existing tests covered it. Commit: eb8fc73.

- Fixed all 17 SonarCloud S3800 issues: replaced mixed-type `load_data.mockImplementation` and `getOption.mockImplementation` callbacks with `mockResolvedValueOnce` chains in tests/apiCall.test.js (15 occurrences) and tests/cache.test.js (2 occurrences). Commits: 27c8d5d, 33fb41b.

- **Gotcha**: `apiCall.js` `data.host` branch calls `authentication()` (and therefore `load_data` for credentials) unless `data.loginflow` is also truthy. A test relying on `mockImplementation` from the previous test (via `vi.clearAllMocks()` not clearing implementations) was silently passing without its own mock — switching to `mockResolvedValueOnce` exposed this, requiring an explicit credentials mock in that test.

- **Gotcha**: `vi.clearAllMocks()` clears mock call history and queued `mockResolvedValueOnce` values, but does NOT reset `mockImplementation`. So tests relying on a prior test's `mockImplementation` bleed through when those tests use `clearAllMocks` in `beforeEach`. Switching from `mockImplementation` to `mockResolvedValueOnce` eliminates this implicit coupling.

- All changes pushed to main on GitHub. Test count: 667 (WSL2 worker timeouts are pre-existing noise, not failures).
